-- 调试相关的自定义配置
-- extras 已在 lua/config/lazy.lua 中按正确顺序导入
return {
  -- 你可以在这里添加额外的调试插件配置或覆盖默认设置
  -- 例如：
  -- {
  --   "mfussenegger/nvim-dap",
  --   opts = function()
  --     -- 自定义 DAP 配置
  --   end,
  -- },
}
